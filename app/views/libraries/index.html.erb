<script src="//maps.google.com/maps/api/js?v=3.13&amp;sensor=false&amp;libraries=geometry" type="text/javascript"></script>
<script src='//google-maps-utility-library-v3.googlecode.com/svn/tags/markerclustererplus/2.0.14/src/markerclusterer_packed.js' type='text/javascript'></script>

<h1>List of Libraries</h1>

<div class="col-md-3">
  <% @libraries.each do |library| %>
    <%= link_to library.name, library_path(library) %><br />
  <% end %>
  <%= link_to 'Add New', new_library_path %>

<!--   Nearby seed libraries:
  <%# for library in @libraries[0].nearbys(500) %>
    <p><%#= library.address %> <%#= library.distance.round(2) %> miles
  <%# end %>
 -->
<%= form_tag libraries_path, :method => :get do %>
  <p>
    <%= text_field_tag :search, params[:search] %>
    <%= submit_tag "Search Near", :name => nil %>
  </p>
<% end %>

</div>




<div class="col-md-9">
  <div id="map-canvas"></div>
</div>

<script type="text/javascript">
  handler = Gmaps.build('Google');
  handler.buildMap({ provider: { zoom: 10, provider_key: "AIzaSyAb6ElpayvwAIdNY3QljaH1zHlahlSCJDI" }, internal: {id: 'map-canvas'}}, function(){
    markers = handler.addMarkers(<%=raw @hash.to_json %>);
    // markers = handler.addMarkers([
    //   {
    //     lat: 0,
    //     lng: 0,
        // picture: {
        //   "url": "https://addons.cdn.mozilla.net/img/uploads/addon_icons/13/13028-64.png",
        //   "width":  36,
        //   "height": 36
        // },
        // "infowindow": "Good morning!"
    //   }
    // ]);
    // handler.map.centerOn(markers[0]);
    
    handler.bounds.extendWith(markers);
    handler.fitMapToBounds();
  });
</script>

